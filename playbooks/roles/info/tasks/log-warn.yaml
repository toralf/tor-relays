---
- name: Grep {{ log_warn }}
  changed_when: false
  ansible.builtin.shell: |
    zgrep -H A 3 ' died' {{ log_warn }}* | tee
  register: result

- name: Results
  ansible.builtin.debug:
    msg: "{{ result.stdout.split('\n') }}"
  when: result.stdout_lines | length > 0
