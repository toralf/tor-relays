---

- name: Get last {{ log_lines }} lines from {{ warn_log }}
  ansible.builtin.shell: |
    /usr/bin/tail -n {{ log_lines }} {{ warn_log }}
  register: result

- name: results
  ansible.builtin.debug:
    msg: "{{ result.stdout.split('\n') }}"
