---
- name: Get hashed fingerprint
  changed_when: false
  ansible.builtin.command: cut -f2 -d' ' /var/lib/tor/hashed-fingerprint
  register: result

- name: Set fact
  ansible.builtin.set_fact:
    hashed_fingerprint: "{{ result.stdout_lines[0] }}"
