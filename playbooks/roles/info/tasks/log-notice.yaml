---
- name: Get last {{ log_lines }} lines from {{ log_notice }}
  changed_when: false
  ansible.builtin.command: /usr/bin/tail -n {{ log_lines }} {{ log_notice }}
  register: result

- name: Results
  ansible.builtin.debug:
    msg: "{{ result.stdout.split('\n') }}"
  when: result.stdout_lines | length > 0
