---

- name: current date
  debug:
    msg: "{{ ansible_date_time.iso8601 }}"

- name: Uname
  ansible.builtin.shell: |
    uname -rv
    uptime
  register: result

- name: result
  ansible.builtin.debug:
    msg: "{{ result.stdout }}"
