---

- name: Get last {{ log_lines }} lines from {{ log_unattended }}
  ansible.builtin.shell: |
    /usr/bin/tail -n {{ log_lines }} {{ log_unattended }} || true
  register: result

- name: results
  ansible.builtin.debug:
    msg: "{{ result.stdout.split('\n') }}"
