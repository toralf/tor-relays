---
- name: ACCEPT TCP to obfs4 port
  ansible.builtin.iptables:
    chain: INPUT
    protocol: tcp
    destination_port: "{{ obfs4_port }}"
    syn: match
    jump: ACCEPT

- name: ACCEPT TCP from Prometheus server to metrics port
  ansible.builtin.iptables:
    chain: INPUT
    protocol: tcp
    source: "{{ prometheus_server }}"
    destination_port: "{{ tor_metrics_port }}"
    syn: match
    jump: ACCEPT
  when: prometheus_server is defined

- name: Save new iptables v4 state
  community.general.iptables_state:
    ip_version: ipv4
    state: saved
    path: /etc/iptables/rules.v4
