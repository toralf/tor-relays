---
- name: Init or update repo
  ansible.builtin.git:
    repo: https://gitlab.torproject.org/tpo/anti-censorship/pluggable-transports/lyrebird
    dest: /root/lyrebird
    version: "{{ lyrebird_git_version }}"
    force: true

- name: Apply Lyrebird patches
  ansible.builtin.shell:
    cmd: curl -s {{ item }} | patch -p 1
    chdir: /root/lyrebird
  loop_control:
    label: "{{ item }}"
  loop: "{{ lyrebird_patches }}"

- name: Build Lyrebird
  ansible.builtin.command:
    cmd: go build ./cmd/lyrebird
    chdir: /root/lyrebird

- name: Copy Lyrebird
  ansible.builtin.copy:
    src: /root/lyrebird/lyrebird
    dest: /usr/bin/
    mode: a+x
    remote_src: yes
  register: _plugin

- name: Set Linux capabilities on Lyrebird
  community.general.capabilities:
    path: /usr/bin/lyrebird
    capability: cap_net_bind_service=+ep
    state: present
  when: _plugin.changed
