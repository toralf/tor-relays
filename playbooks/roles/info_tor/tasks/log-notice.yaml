---
- name: Check notice log
  changed_when: false
  ansible.builtin.shell:
    cmd: |-
      grep -h -v \
        -e 'opening .*log file.' \
        -e 'Please upgrade! This version of Tor' \
        -e 'The IPv4 ORPort address 127.0.0.1 does not match' \
        -e 'Unrecognized BridgeDistribution value' \
        notice.log.1 notice.log |
        tail -n 10 |
        {{ filter_sensitive_data }}
    chdir: /var/log/tor/
    executable: /bin/bash
  register: _result_log_notice

- name: Results of notice log
  when: _result_log_notice.stdout | length > 0
  changed_when: true
  ansible.builtin.debug:
    var: _result_log_notice.stdout_lines
