---
- name: Delete cloud instance
  delegate_to: localhost
  throttle: "{{ concurrent_local_jobs }}"
  changed_when: true
  ansible.builtin.command:
    cmd: >-
      {{ role_path }}/../../../playbooks/bindelete-server.sh {{ inventory_hostname }}
