---
- name: Grep {{ log_warn }}
  changed_when: false
  ansible.builtin.shell: |
    zgrep -H A 3 -e ' died' -e ' denied' -e 'obfs4' {{ log_warn }}* | tee
  register: result

- name: Results of {{ log_warn }}
  ansible.builtin.debug:
    msg: "{{ result.stdout.split('\n') }}"
  when: result.stdout_lines | length > 0
