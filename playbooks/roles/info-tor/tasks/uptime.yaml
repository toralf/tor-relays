---
- name: Get Service Status
  ansible.builtin.systemd:
    name: tor@default.service
  register: tor_status

- name: Uptime
  changed_when: false
  check_mode: false
  ansible.builtin.command: "uptime -ps"
  register: result
  failed_when: result.stdout_lines | length != 1 or result.stderr_lines | length > 0

- name: File results for uptime
  delegate_to: localhost
  throttle: 1
  ansible.builtin.lineinfile:
    create: yes
    line: "{{ ansible_facts.hostname }} TorStart {{ tor_status.status.ExecMainStartTimestamp }} TorReload {{ tor_status.status.StateChangeTimestamp }} uptime {{ result.stdout }}"
    path: "{{ tmp_dir }}/{{ group_names[0] }}_uptime"
    regex: "^{{ ansible_facts.hostname }} "
