---
artefact_paths:
  - /tmp
artefact_patterns:
  - "*"
artefact_script: ""
artefact_size: "200k"
artefact_tmpdir: "{{ tmp_dir }}/artefact"
artefact_cleanup: false

issue_fname_prefix: common
issue_since: 1 day ago
