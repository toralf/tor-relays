---
- name: Ensure nickname length fits
  ansible.builtin.assert:
    quiet: true
    that:
      - "(nickname_prefix + ansible_facts.hostname) | length <= 19"
  when: '"private" in group_names or "public" in group_names'
