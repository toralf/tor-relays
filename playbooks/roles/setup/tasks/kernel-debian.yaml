---
- name: Configure repository "{{ kernel_debian_repo }}"
  ansible.builtin.apt_repository:
    filename: "{{ kernel_debian_repo }}"
    repo: "deb http://deb.debian.org/debian {{ kernel_debian_repo }} main"

- name: Apt pinning for "{{ kernel_debian_repo }}" kernel
  ansible.builtin.copy:
    src: apt-preferences
    dest: /etc/apt/preferences

- name: Install or update "{{ kernel_debian_repo }}" kernel
  ansible.builtin.apt:
    default_release: "{{ kernel_debian_repo }}"
    name:
      - "linux-image-{{ kernel_debian_version }}-{{ ansible_facts.proc_cmdline.BOOT_IMAGE is search('-amd64') | ternary('amd64', 'arm64') }}-unsigned"
