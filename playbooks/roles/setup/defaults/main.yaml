---
additional_ports: []
additional_software: []

# for other kernels than provided by "stable"
kernel_debian_target: "" # e.g. "experimental"
kernel_debian_version: ""

kernel_git_version: "" # e.g. "HEAD" or "v6.7.3"
kernel_git_patches: []
kernel_git_forcebuild: false

metrics_port: ""
prometheus_node_exporter: ""
prometheus_server: ""

# certificates and more
ssl_dir: "{{ playbook_dir }}/../secrets/ssl"
