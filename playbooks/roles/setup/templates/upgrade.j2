# {{ ansible_managed }}

# https://wiki.debian.org/UnattendedUpgrades
[Timer]
OnCalendar=*-*-* {{ 12 | random(seed=inventory_hostname) }},{{ 12 + 12 | random(seed=inventory_hostname) }}:10
RandomizedDelaySec={{ 49 * 60 }}
