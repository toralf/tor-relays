---
lyrebird_git_version: "HEAD"
lyrebird_patches: []

tor_build_from_source: true
tor_git_version: "HEAD"
tor_patches: []

tor_debian_target: "" # if not ansible_facts.distribution_release but e.g. "tor-nightly-main-bookworm"

metrics_port: ""

nickname: ""
