---
log_notice: "/var/log/tor/notice.log"
log_warn: "/var/log/tor/warn.log"
log_err: "/var/log/tor/err.log"

lyrebird_git_version: "HEAD"
lyrebird_patches: []

tor_build_from_source: true
tor_git_version: "HEAD"
tor_patches: []

tor_debian_suite: "{{ ansible_facts.distribution_release }}" # e.g. "tor-nightly-main-bookworm"

metrics_port: ""

nickname: ""
