---
tor_build_from_source: true
lyrebird_git_version: "HEAD"
lyrebird_patches: []
tor_git_version: "HEAD"
tor_patches: []

tor_debian_target: "" # e.g. "tor-nightly-main-bookworm", default is ansible_facts.distribution_release

# torrc values, usually set for an ansible group
tor_config_group: []

# torrc values, usually set for an ansible host
tor_config_host: []

tor_metrics: false

bridge_distribution: ""
