---
- name: Custom command
  block:
    - name: Run a custom command
      changed_when: false
      ansible.builtin.shell:
        cmd: |-
          {{ lookup('ansible.builtin.file', custom_command_file) }}
        executable: /bin/bash
      register: _result_custom_cmd
  rescue:
    - name: Rc of custom command
      failed_when: true
      ansible.builtin.debug:
        var: _result_custom_cmd.rc
  always:
    - name: Stdout of custom command
      when: _result_custom_cmd.stdout | length > 0
      changed_when: true
      ansible.builtin.debug:
        var: _result_custom_cmd.stdout_lines

    - name: Stderr of custom command
      when: _result_custom_cmd.stderr | length > 0
      changed_when: true
      ansible.builtin.debug:
        var: _result_custom_cmd.stderr_lines
