---
log_lines: 20

log_system_since: 1 day ago

issue_fname_prefix: common

nginx_loglevel: error

artefact_paths:
  - /tmp
artefact_patterns:
  - "*"
artefact_script: ""
artefact_size: "200k"
artefact_tmpdir: "{{ tmp_dir }}/artefact"
artefact_cleanup: false
