---
- name: "Ensure that tmp dirs do exist"
  delegate_to: localhost
  run_once: true
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ tmp_dir }}"
    - "{{ tmp_dir }}/issues"
  tags:
    - tmpdir
